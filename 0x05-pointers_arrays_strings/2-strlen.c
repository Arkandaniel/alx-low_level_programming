#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of string
 * @s: character
 * Return: the answer
 */
int _strlen(char *s)
{
	int i;

	i = 0;
	while (s[i] != '\0')
	{
		i++
	}
	return (u);
}
