#include <stdio.h>

/**
 * main - all the single digits integer
 *
 * Return: Always 0
 */
int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
