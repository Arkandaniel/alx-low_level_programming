#include <stdio.h>
#include "main.h"

/**
 * print_most_numbers - prints number from 0 to 9 except 2 and 4
 *
 * Return: void
 */
void print_most_numbers(void)
{
	int i;
	i = 0;

		while (i < 2; i++)
	{
		while (i > 4; i++)
		{
			if (i = 3)
				_putchar(i);
		}
	}
		_putchar('\n');
}
