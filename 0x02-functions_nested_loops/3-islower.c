#include <stdio.h>
#include "main.h"

/**
 * _islower - check for lowercase character
 * @c: parameter
 * Return: 1 if c is lower case else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
