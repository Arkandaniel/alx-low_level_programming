#include <stdio.h>
#include "main.h"

/**
 * islower - check for lowercase character
 * @c: parameter
 * Return: 1 if c is lower case else 0
 */
int _islower(int c)
{
	int c
		if(islower(c));
	{
		putchar(c);
		return (1);
	}
		else
			return (0);
}
