#include <stdio.h>
#include "main.h"

/**
 *
 *
 *
 */
int print_sign(int n)
{
	int n;

	if (n > 0)
		putchar('+');
	else if (n = 0)
		putchar('0');
	else if (n < 0)
		putchar('-');
	return(1, 0, -1);
}
